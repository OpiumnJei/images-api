
# =========================================================================
#  ETAPA 1: LA FÁBRICA 🏭 (Donde construimos la aplicación)
# =========================================================================
# El objetivo aquí es convertir tu código fuente en un archivo .jar ejecutable.
# Usamos herramientas pesadas que no queremos en el producto final.

# Paso 1: Elegir la maquinaria.
# Empezamos con una imagen de Docker que es como un taller completo: ya tiene
# instalado Java 17 y la herramienta de construcción Maven.
# Le ponemos la etiqueta "build" para poder referirnos a esta fábrica más tarde.
FROM maven:3.8-openjdk-17 AS build

# Paso 2: Designar la mesa de trabajo.
# Dentro de la fábrica, creamos una carpeta llamada "/app". A partir de ahora,
# todos los trabajos se realizarán dentro de esta carpeta.
WORKDIR /app

# Paso 3: Traer la lista de materiales.
# Copiamos solo el archivo pom.xml. Este archivo es como la lista de todas
# las piezas de LEGO (dependencias) que nuestro proyecto necesita.
COPY pom.xml .

# Paso 4: Descargar todos los materiales.
# Le decimos a Maven que lea la lista (pom.xml) y descargue todas las piezas
# de internet. Hacemos esto ahora para que Docker pueda reutilizar estas
# piezas descargadas en futuras construcciones si la lista no cambia,
# haciendo todo mucho más rápido. ✨
RUN mvn dependency:go-offline

# Paso 5: Traer las instrucciones de ensamblaje.
# Ahora copiamos nuestro propio código fuente (la carpeta "src") a la fábrica.
COPY src ./src

# Paso 6: Fabricar el producto final.
# Le ordenamos a Maven que compile todo y lo empaquete en un único archivo
# ejecutable (.jar). Esto es como ensamblar todas las piezas de LEGO
# para formar el modelo final.
RUN mvn package -DskipTests


# =========================================================================
#  ETAPA 2: LA CAJA DEL PRODUCTO 🏠 (Donde ejecutamos la aplicación)
# =========================================================================
# Ahora nos olvidamos de la fábrica y sus herramientas. Empezamos con una
# caja limpia y ligera que solo contendrá nuestra aplicación terminada.

# Paso 7: Elegir la base para la caja.
# Empezamos de cero con una imagen súper ligera que solo tiene lo mínimo
# indispensable para ejecutar Java. No hay herramientas de construcción aquí.
FROM openjdk:17-jdk-slim

# Paso 8: Preparar el espacio en la caja.
# Creamos una carpeta "/app" para colocar nuestra aplicación terminada.
WORKDIR /app

# Paso 9: Mover el producto de la fábrica a la caja.
# Este es el paso clave. Copiamos el archivo .jar que creamos en la Etapa 1
# (la fábrica "build") a nuestra caja final y le ponemos un nombre simple: "app.jar".
COPY --from=build /app/target/*.jar app.jar

# Paso 10: Ponerle una "etiqueta" al puerto.
# Le decimos a Docker que nuestra aplicación, una vez encendida, escuchará
# peticiones en el puerto 8080. Es como indicar en la caja "conectar aquí".
EXPOSE 8080

# Paso 11: Escribir las instrucciones de "encendido".
# Este es el comando final que se ejecutará cuando alguien inicie el contenedor.
# Básicamente dice: "Usa Java para encender y ejecutar el archivo app.jar".
ENTRYPOINT ["java", "-jar", "app.jar"]
